node {
    def app
     stage ('Clone'){
        checkout scm
     }
     stage ('Build images'){
        app = docker.build("tag1/image")
     }
     stage ('Test image'){
        docker.image("tag/image").WithRun('-p 80:80'){
            c ->
            sh 'docker ps'
            curl 'localhost'
            }
     }
}
